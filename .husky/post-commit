#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

if [ "$CHANGELOG_SKIP" = "1" ]; then
  exit 0
fi

node scripts/changelog-sync.mjs || exit 0

if ! git diff --quiet -- CHANGELOG.md; then
  git add CHANGELOG.md
  CHANGELOG_SKIP=1 git commit --amend --no-edit --no-verify
fi